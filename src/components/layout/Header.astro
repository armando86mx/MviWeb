---
import { Image } from 'astro:assets';
import { NAV_PRIMARY, NAV_CTA, SITE } from '@lib/site';
import Button from '@components/ui/Button.astro';
import logo from '@assets/logos/mvi-logo-full.png';

const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';

const isActivePath = (href: string) => {
  const cleanHref = href.replace(/\/$/, '') || '/';
  return cleanHref === pathname || (cleanHref !== '/' && pathname.startsWith(cleanHref));
};

const isCtaActive = isActivePath(NAV_CTA.href);
---

<header class="site-header" data-header>
  <div class="container site-header__inner">
    <a href="/" class="brand" aria-label={`${SITE.name} — inicio`}>
      {/* 220px = tamaño real renderizado (CSS height 44px); densities 2x cubre retina */}
      <Image src={logo} alt={SITE.name} class="brand__logo" width={220} height={44} densities={[1, 2, 3]} loading="eager" />
    </a>

    <nav aria-label="Navegación principal" class="nav">
      <ul class="nav__list">
        {NAV_PRIMARY.map(({ label, href }) => {
          const isActive = isActivePath(href);
          return (
            <li>
              <a href={href} class={`nav__link ${isActive ? 'is-active' : ''}`} aria-current={isActive ? 'page' : undefined}>{label}</a>
            </li>
          );
        })}
        <li class="nav__cta">
          <Button href={NAV_CTA.href} variant="primary" size="sm" class={isCtaActive ? 'is-current' : ''} aria-current={isCtaActive ? 'page' : undefined}>{NAV_CTA.label}</Button>
        </li>
      </ul>
    </nav>

    <button class="nav-toggle" aria-expanded="false" aria-controls="mobile-nav" aria-label="Abrir menú" data-nav-toggle>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </button>
  </div>

  <nav id="mobile-nav" class="mobile-nav" aria-label="Navegación móvil" data-mobile-nav hidden>
    <ul class="mobile-nav__list">
      {NAV_PRIMARY.map(({ label, href }) => {
        const isActive = isActivePath(href);
        return (
          <li>
            <a
              href={href}
              class={`mobile-nav__link ${isActive ? 'is-active' : ''}`}
              aria-current={isActive ? 'page' : undefined}
            >{label}</a>
          </li>
        );
      })}
      <li class="mobile-nav__cta">
        <Button href={NAV_CTA.href} variant="primary" fullWidth class={isCtaActive ? 'is-current' : ''} aria-current={isCtaActive ? 'page' : undefined}>{NAV_CTA.label}</Button>
      </li>
    </ul>
  </nav>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: var(--z-header);
    background-color: var(--paper-400);
    backdrop-filter: saturate(180%) blur(10px);
    -webkit-backdrop-filter: saturate(180%) blur(10px);
    border-bottom: 1px solid rgba(80,70,50,0.1);
    transition: background-color var(--dur-2) var(--ease-out), box-shadow var(--dur-2) var(--ease-out);
  }
  .site-header.is-scrolled {
    background-color: rgba(239,234,224,0.95);
    box-shadow: var(--shadow-sm);
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--sp-5);
    min-height: var(--header-h);
    padding-block: 18px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
  }
  .brand__logo { flex: 0 0 auto; height: 44px; width: auto; display: block; }
  @media (max-width: 420px) {
    .brand__logo { height: 38px; }
  }

  .nav { display: none; }
  .nav__list {
    display: flex;
    align-items: center;
    gap: var(--sp-5);
    list-style: none;
    margin: 0; padding: 0;
  }
  .nav__link {
    font-family: var(--font-body);
    font-size: var(--fs-sm);
    font-weight: var(--fw-medium);
    text-decoration: none;
    color: var(--nav-text);
    padding: 0.5rem 0;
    white-space: nowrap; /* un item partido en 2 líneas rompe la barra */
    transition: color var(--dur-link) var(--ease);
  }
  @media (hover: hover) and (pointer: fine) {
    .nav__link:hover { color: var(--ochre-700); }
  }
  .nav__link.is-active {
    font-weight: var(--fw-semibold);
    color: var(--blue-700);
    border-bottom: 2px solid var(--amber-500);
    padding-bottom: 3px;
  }
  .nav__cta { margin-left: var(--sp-2); }

  /* CTA activo (estás en /contacto): anillo ámbar discreto dentro del pill.
     El hover conserva la firma navy→ámbar (esta regla pisa a la de Button). */
  :global(.btn.is-current) {
    box-shadow: inset 0 0 0 2px var(--amber-500), var(--shadow-btn-hero);
  }
  @media (hover: hover) and (pointer: fine) {
    :global(.btn.is-current:hover) {
      box-shadow: inset 0 0 0 2px var(--amber-500), var(--shadow-btn-hero-hover);
    }
  }

  .nav-toggle {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;
    /* Target táctil ≥ 44×44 px */
    min-width: 44px;
    min-height: 44px;
    padding: 10px;
    border-radius: var(--radius-sm);
  }
  .nav-toggle span {
    width: 22px;
    height: 2px;
    background-color: var(--blue-700);
    border-radius: 2px;
    transition: transform var(--dur-2) var(--ease-out), opacity var(--dur-1) var(--ease-out);
  }
  .nav-toggle[aria-expanded="true"] span:nth-child(1) { transform: translateY(6px) rotate(45deg); }
  .nav-toggle[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .nav-toggle[aria-expanded="true"] span:nth-child(3) { transform: translateY(-6px) rotate(-45deg); }

  .mobile-nav {
    /* Capa sobre el contenido (no empuja el sitio hacia abajo):
       el header sticky es el bloque contenedor */
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    border-top: 1px solid var(--paper-line);
    background: var(--paper-200);
    box-shadow: var(--shadow-lg);
    /* En landscape el panel puede ser más alto que el viewport:
       que scrollee él, no que esconda items */
    max-height: calc(100dvh - var(--header-h));
    overflow-y: auto;
    overscroll-behavior: contain;
    /* Entrada suave (progresiva: navegadores sin @starting-style abren al instante) */
    opacity: 1;
    translate: 0 0;
    transition: opacity var(--dur-2) var(--ease-out), translate var(--dur-2) var(--ease-out);
  }
  @starting-style {
    .mobile-nav { opacity: 0; translate: 0 -6px; }
  }
  .mobile-nav[hidden] { display: none; }
  .mobile-nav__list {
    display: flex;
    flex-direction: column;
    list-style: none;
    margin: 0; padding: var(--sp-2) var(--container-pad) var(--sp-5);
  }
  .mobile-nav__link {
    display: block;
    padding: var(--sp-4) 0;
    font-size: var(--fs-md);
    font-weight: var(--fw-medium);
    text-decoration: none;
    color: var(--nav-text);
    border-bottom: 1px solid var(--paper-line);
    transition: color var(--dur-link) var(--ease);
  }
  @media (hover: hover) and (pointer: fine) {
    .mobile-nav__link:hover { color: var(--ochre-700); }
  }
  .mobile-nav__link.is-active {
    color: var(--blue-700);
    font-weight: var(--fw-semibold);
    box-shadow: inset 3px 0 0 var(--amber-500);
    padding-left: var(--sp-3);
  }
  .mobile-nav__cta { padding-top: var(--sp-4); }

  /* 940: bajo ese ancho el nav completo no cabe en una fila */
  @media (min-width: 940px) {
    .nav { display: block; }
    .nav-toggle { display: none; }
    .mobile-nav { display: none !important; }
  }
</style>

<script>
  // Sticky header shadow + menú móvil (toggle, Escape, clic fuera)
  const header = document.querySelector('[data-header]');
  const toggle = document.querySelector('[data-nav-toggle]') as HTMLButtonElement | null;
  const mobile = document.querySelector('[data-mobile-nav]') as HTMLElement | null;

  if (header) {
    const onScroll = () => {
      header.classList.toggle('is-scrolled', window.scrollY > 4);
    };
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
  }

  if (toggle && mobile) {
    const setOpen = (open: boolean) => {
      toggle.setAttribute('aria-expanded', String(open));
      toggle.setAttribute('aria-label', open ? 'Cerrar menú' : 'Abrir menú');
      const teniaFoco = mobile.contains(document.activeElement);
      mobile.hidden = !open;
      // Con el menú abierto el fondo no se desliza
      document.documentElement.style.overflow = open ? 'hidden' : '';
      // Foco: al abrir entra al panel; al cerrar (si estaba dentro) vuelve al botón.
      // Si el toggle ya no es visible (cruce al layout de escritorio), al nav.
      if (open) mobile.querySelector('a')?.focus();
      else if (teniaFoco) {
        if (toggle.offsetParent) toggle.focus();
        else (document.querySelector('.nav__link') as HTMLElement | null)?.focus();
      }
    };

    toggle.addEventListener('click', () => {
      const expanded = toggle.getAttribute('aria-expanded') === 'true';
      setOpen(!expanded);
    });

    // Escape cierra y devuelve el foco al botón
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !mobile.hidden) {
        setOpen(false);
        toggle.focus();
      }
    });

    // Clic fuera del header cierra el panel
    document.addEventListener('click', (e) => {
      if (mobile.hidden) return;
      const target = e.target as Node;
      if (header && !header.contains(target)) setOpen(false);
    });

    // Al pasar al layout de escritorio (p. ej. rotar a landscape) el menú
    // se oculta por CSS: cerrar de verdad para no dejar el scroll bloqueado
    window.matchMedia('(min-width: 940px)').addEventListener('change', (e) => {
      if (e.matches && !mobile.hidden) setOpen(false);
    });
  }
</script>
